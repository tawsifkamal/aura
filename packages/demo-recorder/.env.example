# packages/demo-recorder/.env.example
# Copy to packages/demo-recorder/.env and fill in your values.
# This file is also used by the demo-notebook (it loads this path directly).

# -----------------------------
# browser-use
# -----------------------------
BROWSER_USE_API_KEY=          # Get from https://browser-use.com

# -----------------------------
# LLM Provider (pick one)
# -----------------------------
OPENAI_API_KEY=               # https://platform.openai.com/api-keys
# ANTHROPIC_API_KEY=          # https://console.anthropic.com/keys
# GOOGLE_API_KEY=             # https://aistudio.google.com/apikey

# -----------------------------
# Convex (backend for the dashboard)
# -----------------------------
NEXT_PUBLIC_CONVEX_URL=       # e.g. https://your-deployment.convex.cloud/
CONVEX_DEPLOY_KEY=            # npx convex dev → copies to .env.local automatically

# -----------------------------
# App
# -----------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
