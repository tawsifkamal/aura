[project]
name = "demo-notebook"
version = "0.1.0"
description = "Demo notebook for exploring the demo-recorder with browser-use"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.0.0",
    "ipykernel>=6.0.0",
    "browser-use[video]>=0.2.0",
    "langchain-openai>=0.3.0",
    "langchain-anthropic>=0.3.0",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
demo-recorder = { path = "../demo-recorder" }
