# packages/demo-notebook/.env.example
# Copy to packages/demo-notebook/.env and fill in your values.
# The notebook loads THIS file (not the system env) via python-dotenv.
# Alternatively, just point it at packages/demo-recorder/.env — same keys.

BROWSER_USE_API_KEY=          # Get from https://browser-use.com
OPENAI_API_KEY=               # https://platform.openai.com/api-keys
