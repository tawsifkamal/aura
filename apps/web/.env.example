# apps/web/.env.example
# Copy to apps/web/.env.local and fill in your values.
# Next.js loads .env.local automatically — do not commit .env.local.

# -----------------------------
# Convex (required for the recordings dashboard)
# -----------------------------
NEXT_PUBLIC_CONVEX_URL=       # e.g. https://your-deployment.convex.cloud/
                              # Run `npx convex dev` once and it sets this automatically.
